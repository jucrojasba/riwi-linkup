// Represents the proficiency level of a language
export interface ILanguageLevel {
    id: number;           // Unique identifier for the language level
    levelId: number;     // Identifier for the proficiency level
    languageName: string; // Name of the language associated with this level
}
