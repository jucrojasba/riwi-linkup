// Interface representing a soft skill entity
export interface ISoftSkill {
    id: number; // Unique identifier for the soft skill
    name: string; // Name of the soft skill (e.g., Communication, Teamwork, Problem-solving)
}
